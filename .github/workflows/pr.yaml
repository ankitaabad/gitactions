name: test workflow

on: 
  pull_request:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu_latest
    steps:
      - name: checkout code
        uses: actions/checkout@v2

      - name: print hello world
        run: echo "hello world"
